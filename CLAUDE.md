# CLAUDE.md — Token 使用效率指南

## 目前的暫時性設定

- **Varipulse Adoption Rate 已隱藏**（2026-07-28 起，三個分頁都是）。
  要加回來：把 `build_dashboard.py` 的 `SHOW_VARIPULSE_ADOPTION` 改成 `True`，
  再跑 `python3 build_dashboard.py --push`。計算邏輯和模板 markup 都還在，不用改別的。

---

## 開始新任務前的自我檢查

**如果這個任務預計需要大量來回修改（超過 10 輪對話），請先問我：**
> 「這個任務範圍較大，要不要先開新 session 再開始，避免 context 累積？」

---

## 省 token 的核心原則

### 1. 用 str_replace，不要重寫整份檔案
修改檔案時只傳送變更的片段，不重新輸出完整內容。

### 2. 用 view 指定行號範圍
```
view file.html [20, 80]   ← 只讀需要的段落
```
不要每次都讀整份檔案。

### 3. 不需要解釋你做了什麼
除非我要求，否則直接修改，不附說明文字。

### 4. 一個介面完成後再換下一個
不要同時處理多個檔案或多個功能，完成一個再換下一個，
做完就建議我開新 session。

### 5. 靠檔案當記憶，不靠對話歷史
每次開新 session，直接重新 view 檔案即可，不需要我重新描述。

---

## 我的責任

- 一次把所有需求說清楚，不要一條一條分開說
- 每個功能做完就開新 session，讓 context 歸零
- 不要讓你反覆確認，描述清楚再開始

---

## 何時建議我開新 session

請在以下情況主動提醒我：
- 預計修改超過 3 個不同介面或功能
- 對話已經很長，context 開始累積
- 一個完整功能剛做完，即將進入下一個

提示語範例：
> 「這個功能已完成。接下來的 X 工作範圍較大，建議開新 session 再繼續。」
